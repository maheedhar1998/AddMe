import { FormControl } from '@angular/forms'

export class UsernameValidator {
    static validUsername(formControl: FormControl)
    {
        // TODO - Validate that username is available
        return null
    }
}